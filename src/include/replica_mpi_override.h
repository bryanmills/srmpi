#ifndef REPLICA_MPI_OVERRIDE_H
#define REPLICA_MPI_OVERRIDE_H
#include "mpi.h"
#include <stdio.h>
#include <stdlib.h>
#include <assert.h>

#include "list.h"

/* this is a flag to choose the type of replication:
 * 
 * parallel - splits the set into two independent groups to do the
 * work
 */
#define IS_PARALLEL 0

// override functions

#endif
